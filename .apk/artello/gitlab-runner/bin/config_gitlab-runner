#!/bin/sh

keys="
  CI_SERVER_URL
  REGISTRATION_TOKEN
  RUNNER_TAG_LIST
"

for k in ${keys}; do
  export ${k}=$(curl -s --unix-socket /dev/lxd/sock x/1.0/config/user.${k});
done

export HOME=/home/builder